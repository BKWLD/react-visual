import {
  imageAsset,
  portraitImageAsset,
  videoAsset,
  portraitVideoAsset,
} from "./assets";

export const visualEntry = {
  image: imageAsset,
  portraitImage: portraitImageAsset,
  video: videoAsset,
  portraitVideo: portraitVideoAsset,
  alt: "Description",
};
